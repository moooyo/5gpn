# new-5gpn — bogus-nxdomain poison list (included by smartdns.conf via conf-file).
#
# Some ISPs / the GFW answer blocked or non-existent domains with a junk "landing"
# IP instead of NXDOMAIN. List those poison IPs here (one directive per line) so
# smartdns returns SOA instead of the junk address. Edit, then restart smartdns.
#
# This file ships empty (comments only) — add real poison IPs as you observe them:
#   bogus-nxdomain 1.2.3.4
#   bogus-nxdomain 243.185.187.0/24
