#!/usr/bin/env bash
# RETIRED (Task 8): smartdns replaced by 5gpn-dns.
# render_smartdns_conf.py and etc/smartdns.conf.template were deleted.
# All checks in this file are obsolete. The file is kept for git history.
echo "smartdns conf policy: SKIP (smartdns removed — see test_5gpndns_policy.sh)"
exit 0
